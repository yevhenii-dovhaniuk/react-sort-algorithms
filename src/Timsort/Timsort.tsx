import React, { FunctionComponent } from 'react';

interface OwnProps {
}

type Props = OwnProps;

const Timsort: FunctionComponent<Props> = (props) => {

  return (<div className="timsort">Timsort Impl</div>);
};

export default Timsort;
