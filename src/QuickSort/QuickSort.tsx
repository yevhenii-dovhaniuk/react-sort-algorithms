import React, { FunctionComponent } from 'react';

interface OwnProps {
}

type Props = OwnProps;

const QuickSort: FunctionComponent<Props> = (props) => {

  return (<div className="quick-sort">Quick Sort Impl Here</div>);
};

export default QuickSort;
